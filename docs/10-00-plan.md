# Plan

## Purpose

This plan records current and future owner work for `tool.scad-project`.
Released history belongs in [../CHANGELOG.md](../CHANGELOG.md).

## Current position

The canonical generic `update.*` ownership cleanup (#107) and numbered
documentation structure (#110) are complete on current main.

Issue #112 is complete and qualified on exact main. The current Migration-011
owner step is the `v0.15.11` release candidate, built on the accepted
`tool.git-project v0.2.13` baseline and the shared
`build` / `verify` / `ci` / `release` / `pages` workflow vocabulary.

Consumer rollout starts only after this exact release candidate is qualified on
PR head and main, tagged, and the tagged owner test succeeds.

## Working priorities

When changing this tool:

1. keep generic repository mechanics in `tool.git-project`;
2. keep SCAD project/build/design/verification semantics here;
3. keep runtime implementation in the qualified SCAD toolchain image family;
4. keep consumer workflow callers thin and released;
5. preserve direct and SCons behavior where both are supported;
6. retain exact evidence for release-visible contracts.

## Information sources

| Question | Authority |
| --- | --- |
| How to develop/release this repository | [20-01-development.md](20-01-development.md) |
| How consumers use it | [20-02-user.md](20-02-user.md) |
| Public SCAD contracts | [30-00-specification.md](30-00-specification.md) |
| Internal ownership/design | [40-00-design.md](40-00-design.md) |
| Verification strategy | [50-00-verification.md](50-00-verification.md) |
| Completed history | [../CHANGELOG.md](../CHANGELOG.md) |
| Exact current state | source, config, workflows, tests and live Actions |
