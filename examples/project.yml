schema_version: 1

project:
  name: example.scad-project

profiles:
  - type: scad
    config: project.scad.yml

dependencies:
  - name: tool.scad-project
    role: tooling
    type: git-submodule
    url: https://github.com/brainboxemb/tool.scad-project.git
    path: tools/tool.scad-project
    # Use an immutable release tag or full commit SHA in real consumers.
    ref: main
